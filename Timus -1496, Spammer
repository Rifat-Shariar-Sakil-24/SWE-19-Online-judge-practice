#include<bits/stdc++.h>
#define ll  long long
#define pb push_back
#define sp " "
#define maxi 1000000000
#define forn(i,n) for(long long i=0;i<n;i++)
using namespace std;
 
int main()
{ 
    ll n ; cin >> n;
    map <string,ll> mp;
    while(n--)
    {
        string s; cin >> s;
        mp[s]++;
    }
    for(auto it = mp.begin();it!=mp.end();it++)
    {
        if(mp[it->first]>=2) cout << it->first << endl;
    }

}
